import TableContainer from '@material-ui/core/TableContainer';
import styled from 'styled-components';

export default styled(TableContainer)`
  .custom-tabel__pagination {
    display: flex;
    justify-content: flex-end;
  }
`;
